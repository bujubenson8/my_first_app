import 'package:flutter/material.dart';

class ListViewsPage extends StatelessWidget {
  const ListViewsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: ListView(
        children: [
          Container(
            width: 50,
            height: 50,
            margin: EdgeInsets.all(20),
            decoration: BoxDecoration(color: Colors.amber),
            child: Row(children: []),
          ),

          Container(
            width: 50,
            height: 50,
            margin: EdgeInsets.all(20),
            decoration: BoxDecoration(color: Colors.amber),
            child: Row(children: []),
          ),

          Container(
            width: 50,
            height: 50,
            margin: EdgeInsets.all(20),
            decoration: BoxDecoration(color: Colors.amber),
            child: Row(children: []),
          ),

          Container(
            width: 50,
            height: 50,
            margin: EdgeInsets.all(20),
            decoration: BoxDecoration(color: Colors.amber),
            child: Row(children: []),
          ),

          Container(
            width: 50,
            height: 50,
            margin: EdgeInsets.all(20),
            decoration: BoxDecoration(color: Colors.amber),
            child: Row(children: []),
          ),

          Container(
            width: 50,
            height: 50,
            margin: EdgeInsets.all(20),
            decoration: BoxDecoration(color: Colors.amber),
            child: Row(children: []),
          ),

          Container(
            width: 50,
            height: 50,
            margin: EdgeInsets.all(20),
            decoration: BoxDecoration(color: Colors.amber),
            child: Row(children: []),
          ),
          Container(
            width: 50,
            height: 50,
            margin: EdgeInsets.all(20),
            decoration: BoxDecoration(color: Colors.amber),
            child: Row(children: []),
          ),

          Container(
            width: 50,
            height: 50,
            margin: EdgeInsets.all(20),
            decoration: BoxDecoration(color: Colors.amber),
            child: Row(children: []),
          ),
        ],
      ),
    );
  }
}
